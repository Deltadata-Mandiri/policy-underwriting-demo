// Local-dev default: server.js serves the UI and /api/* from the same origin,
// so no base URL is needed. An Amplify build can overwrite this file with a
// deployed Lambda Function URL.
window.APP_CONFIG = { apiBase: '' };
